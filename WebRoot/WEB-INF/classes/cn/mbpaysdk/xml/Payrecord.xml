<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="Payrecord">

	<typeAlias alias="payrecord" type="cn.mbpaysdk.vo.PayrecordVO" />
	
<!-- 
	<select id="getByOrderid" parameterClass="string" resultClass="payrecord">
		select * from  mbpaysdk_pay_record where orderid = #VARCHAR# order by id desc limit 1
	</select>
	
	<select id="getByTid" parameterClass="string" resultClass="payrecord">
		select * from  mbpaysdk_pay_record where tid = #VARCHAR# order by id desc limit 1
	</select>
	
	<select id="getBySMSID" parameterClass="string" resultClass="payrecord">
		select * from  mbpaysdk_pay_record where SMSID = #VARCHAR#
	</select>
	
	<select id="getById" parameterClass="payrecord" resultClass="payrecord">
		select * from  mbpaysdk_pay_record where id = #id:INT#
	</select>
	
	<select id="getByChannelOrderid" parameterClass="string" resultClass="payrecord">
		select * from  mbpaysdk_pay_record where channel_order_id = #VARCHAR# order by id desc limit 1
	</select>
	 -->
</sqlMap>
